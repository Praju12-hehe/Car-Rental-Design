{
  "buildCommand": "pnpm install && pnpm run build",
  "outputDirectory": "artifacts/varda/dist/public",
  "installCommand": "pnpm install",
  "env": {
    "PORT": "3000",
    "BASE_PATH": "/"
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
